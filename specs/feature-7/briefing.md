* add a reveal button after 3 failed attempts, to give up and reveal the solution
* auto-select the root after the 2nd try, since it's shown with the nudge anyway
* add the tempo to the card again
* add tooltip to the 3 dots, explaining you have 3 tries (but actually unlimited)
* flavour is unclear: I think we should only use modes (ionian, dorian, ...)
* add a simple mode (toggle on top of guessing card). In simple mode, only show 6 options for the root and reduce the selected mode to the choice between major and minor.
* repeat frequency: let's change the algorhithm to decide the groove.  I want all 16 (or whatever amount of grooves I have) to be selected once before repeating
