* add five feels from [new-styles.md](../../new-styles.md): Bossa Nova, Afro-Cuban son montuno, reggae one-drop, second line / New Orleans, boom-bap
* drop the rule that each feel owns exactly two flavours and the six pairs are disjoint
* each style owns two to four modes instead, and the sets may overlap. a feel is still a clue to the mode, just a weaker one
* what stays: every flavour the game offers has grooves behind it, and no groove answers to a flavour the game does not offer
* the three tests and the sentence in `docs/music.md` change with the rule
* existing templates keep their `flavours` lists as they are — reordering or removing an entry re-renders that template's grooves and reassigns past puzzles; adding to a list appends
* new-styles.md's table names each style's sound on our voices and its modes — the starting point, not the final decision. the `musician` decides each template's parameters
* a template is tempo, swing, subdivision, voices, gains, pans, a humanize block, placements, a fill vocabulary, minted grooves, a re-rendered manifest and a listening sign-off
* son montuno needs the claves, cowbell and bongos-as-lead that feature-24 sources, so it lands after 24
* feature-24 buys those voices but plays none of them — the first style that reaches for the claves owns their listening pass, and should budget for re-sourcing one voice if a bare wood transient machine-guns over a four-bar loop
* out of scope: everything under "Not cheap" in new-styles.md — 12-bar blues, jazz waltz, 6/8 Afro, gospel 12/8. they stay in the file as candidates
