* a transpose pill in the header next to the share button and the streak, looking like them, not like text: tap it to cycle Concert → E♭ alto sax → B♭ tenor & trumpet
* it reads "Transpose" while nothing is selected, so a sax player knows what it is for; once set it reads the instrument
* one line in the how-to-play box saying what Transpose does, the same way the box names Simple mode
* persisted as a preference, like simple mode and tap sounds
* the whole puzzle follows the chosen instrument's written pitch, not just the reveal
* the guess boxes look exactly the same; only what the chips mean changes — on E♭, tapping the C chip plays the sound an alto sax makes when it fingers C, a concert E♭, so the chip and the sound agree
* the check, the nudges and the heading follow the same reading: an alto player solving a concert E♭ groove guesses C and is told C
* re-spells the lead-sheet chords for the chosen instrument
* re-spells the staff notes and the staff label for the chosen instrument
* the transport panel's chord line follows the same setting
* the audio and the share link stay in concert pitch — sound is sound, and the link opens the same groove for anyone
* no fretboard or keyboard picture; the register above the comp stays Sam's
* Sam plays guitar and alto sax (docs/persona.md); the alto sax reads a major sixth up
