* the three attempt dots are confusing — remove them
* a puzzle counts as solved when you solve it, however many tries it took
* giving up, or not attempting a puzzle at all, resets the streak to 0
* keep storing the attempts in `localStorage` — they might feed stats later
* check every hint and other wording for snippets that need changing now that the three attempts are gone
