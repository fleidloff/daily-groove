* make sure the app runs on vercel
* add supabase for storing user data
* still no authentication. Users only get an ID assigned that is stored in localhost, data is requested based on that id
* add security so that an id cannot be stolen
* allow to export your data and import it in another browser
* guide me on all the manual steps required in vercel and supabase
* add links to all relevant stuff in README
